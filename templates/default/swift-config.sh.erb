# Generated by Chef
 
# The name of the Swift container in which to store backups.
# Should be one per backup set.
# Must be unique across *all* Swift container names within the 
# configured OpenStack tenancy.
OS_CONTAINER="<%= @os_container %>"
 
REPOSITORY_URL="swift://$OS_CONTAINER"
 
FULL_BACKUPS_TO_KEEP="<%= @full_backups_to_keep %>"
 
. <%= @duplicity_dir %>/keys.sh
